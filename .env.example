# Neon PostgreSQL connection string. Create/select a database named master_db in Neon.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/master_db?sslmode=require"

# Change this to a long random value before deploying.
SESSION_SECRET="change-this-secret"

# Optional server port.
PORT=3000
